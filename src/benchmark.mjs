import fs from 'node:fs';import {analyze} from './impact.mjs';
const rows=['org-a','org-b'].map(org=>{const root='examples/'+org,map=JSON.parse(fs.readFileSync(root+'/tests.json')),oracle=JSON.parse(fs.readFileSync(root+'/oracle.json')),report=analyze(root+'/base',root+'/head',map);return {org,evidence_status:'simulated',baseline:'all declared test IDs',baselineCount:map.tests.length,selectedCount:report.selectedTests.length,retainedKnownAffected:oracle.knownAffectedTests.filter(id=>report.selectedTests.includes(id)).length,knownAffected:oracle.knownAffectedTests.length,extractedEdges:report.metrics.extractedEdges,nativeTestsExecuted:0,methodAdvantage:'not established',oracleAuthor:'AI internal'};});
console.log(JSON.stringify({protocol:'examples/protocol.json',rows},null,2));
