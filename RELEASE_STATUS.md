# Salesforce experimental source preview

The primary implementation is a Salesforce DX project with Apex/Flow or a Salesforce-specific Node metadata adapter. Python is historical only under legacy/python-prototype. Local package checks and a rollback-only native Developer-org validation passed for the scoped source. No deployment was saved. Full original acceptance remains partial; read module.json.

A native fixture/unit test pass is not independent evidence, complete Agentforce/FSC integration, a supported production release, adoption or originality. Optional integrations and multi-org behavioral acceptance remain explicit gaps.
